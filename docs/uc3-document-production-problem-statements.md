# Current document production problem statements

| Problem Number | Problem statement | Tag |
| ---: | --- | --- |
| 1 | Source files, templates, assets and previous outputs are spread across shared drives, legacy systems and external storage, making them slow or costly to find. | `prototype in production` |
| 2 | Multiple old and new job identifiers make it difficult to trace a request through to its working files and final document. | `prototype in production` |
| 3 | Each job starts with manually copying folders, reference files and prior working documents; missing source files can force a rebuild. | `prototype in production` |
| 4 | The same copy, date or product-data change must be applied separately to every affected document and variant, so effort grows with document count. | `prototype in production` |
| 5 | PDF comments cannot always be imported into the original InDesign file, forcing changes to be read and applied manually. | `prototype in production` |
| 6 | Structured values and tables are manually copied from spreadsheets into documents, increasing effort and error risk. | `prototype in production` |
| 7 | Changes made only in Acrobat can diverge from the InDesign source and disappear during a later update. | `prototype in production` |
| 8 | Work is difficult to hand over because production context and revision history are not held in one shared source. | `prototype in production` |
| 9 | Reusable layouts and form controls are manually copied from template pages, and correct use depends on specialist knowledge. | `prototype in production` |
| 10 | Text and layout changes can move interactive fields, requiring every field, tab order, link, option, comb length and calculation to be checked manually. | `prototype in production` |
| 11 | Working, review, editable and final versions are renamed, converted, reader-enabled and uploaded through several manual steps. | `prototype in production` |
| 12 | There is no reliable inventory of documents that are live, redundant, missing source files or still require updates. | `update request triage and drafting` |
| 13 | Ownership is incomplete or unclear for parts of the document estate and for the future production process. | `update request triage and drafting` |
| 14 | Requests are inconsistently named and labelled, and often lack the metadata needed to filter, prioritise, measure or assign the work. | `update request triage and drafting` |
| 15 | Briefs and markups are often incomplete or ambiguous, causing missed requirements, clarification calls and additional revision rounds. | `update request triage and drafting` |
| 16 | Production specialists are expected to interpret content changes even though they are not the subject-matter owners. | `update request triage and drafting` |
| 17 | Update instructions arrive across markups, spreadsheets and other references rather than as one approved, structured change set. | `update request triage and drafting` |
| 18 | Some legacy working files must be recovered from external storage before an update can begin, sometimes at additional cost. | `update request triage and drafting` |
| 19 | Release dates and briefing schedules are not always confirmed early enough, creating large batches of urgent work. | `update request triage and drafting` |
| 20 | Approval, hand-off and future-system responsibilities are not consistently defined across the end-to-end process. | `update request triage and drafting` |